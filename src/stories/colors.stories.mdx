import { Meta } from '@storybook/addon-docs';
import toast from '../components/Toast';

<script>
const colors = {
    primary: {
        100: #d3d9cc,
        200: #c6ccc0,
        300: #babfb4,
        400: #adb3a8,
        500: #9aa692,
        600: #809a6f,
        700: #5b734d,
        800: #394d2f,
        900: #1b2616,
    },
    secondary: {
        100: #fff9f0,
        200: #f2ece4,
        300: #e6d9cc,
        400: #d9bb9e,
        500: #cc9c75,
        600: #a67756,
        700: #80563c,
        800: #593825,
        900: #331e15,
    },
    error: {
        50: #ffd5d5,
        100: #ffbaba,
        200: #ff9595,
        300: #ff7c7c,
        400: #fd6b6b,
        500: #ff5252,
        600: #f24040,
        700: #d82e2e,
        800: #bf1f1f,
        900: #a40b0b,
        1000: #7e0202,
    },
    grey: {
        100: #f5f5f5,
        150: #f3f3f3,
        200: #e0e0e0,
        300: #c3c3c3,
        400: #a5a5a5,
        500: #7b7b7b,
        600: #575757,
        700: #3a3a3a,
        800: #272727,
        900: #171717,
    }
}
</script>

<Meta title="컬러 시스템" />

<style>{`
  .container {
    display: flex;
    flex-direction: column;
    width: 100%;
    max-height: 85vh;
    overflow-y: scroll;
    row-gap: 32px;
    }

    .row {
        display: flex;
        flex-direction: column;
        width: 100%;
    }

    .title {
        margin-bottom: 16px;
        text-align: left;
        font-weight: 500;
        font-size: 24px;
        line-height: 35px;
        letter-spacing: -0.03em;
        color: #000000;
    }

    ol {
        display: flex;
        padding: 0px !important;
        margin: 0px !important;
    }

    li {
        display: flex;
        align-items: center;
        flex-direction: column;
        row-gap: 12px;
        margin-top: 0px !important;
    }

    li > div {
        width: 80px;
        height: 80px;
        transition: transform 0.2s;
        cursor: pointer;
    }

    li > div:hover {
        transform: scale(1.1);
        transition: transform 0.2s;
    }

    span {
        font-style: normal;
        font-weight: 500;
        font-size: 24px;
        line-height: 35px;
        text-align: center;
        letter-spacing: -0.03em;
        color: #000000;
    }
`}</style>

<div class="container">
    <div class="row">
        <h1 class="title">Primary</h1>
        <ol>
            {Object.entries(colors.primary).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Blue : Dynamic</h1>
        <ol>
            {Object.entries(colors.blueDynamic).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Blue : Soft</h1>
        <ol>
            {Object.entries(colors.blueSoft).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Error</h1>
        <ol>
            {Object.entries(colors.error).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Red : Dynamic</h1>
        <ol>
            {Object.entries(colors.redDynamic).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Red : Soft</h1>
        <ol>
            {Object.entries(colors.redSoft).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Orange : Dynamic</h1>
        <ol>
            {Object.entries(colors.orangeDynamic).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Green : Dynamic</h1>
        <ol>
            {Object.entries(colors.greenDynamic).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Green : Soft</h1>
        <ol>
            {Object.entries(colors.greenSoft).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="row">
        <h1 class="title">Grey</h1>
        <ol>
            {Object.entries(colors.grey).map(([key, value]) => (
                <li
                    key={key}
                    onClick={() => {
                        copyToClipboard(value);
                        toast.success(`${value} 복사 되었습니다.`);
                    }}
                >
                    <div style={{ backgroundColor: value }}></div>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
</div>
