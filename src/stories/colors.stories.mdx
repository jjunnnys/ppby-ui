import { Meta } from '@storybook/addon-docs';
import colors from '@field-share/colors';
import { copyToClipboard } from '@field-share/utils';

<Meta title="컬러 시스템" />

<style>{`
  .container {
    display: flex;
    flex-wrap: wrap;
    column-gap: 20px;
  }

  .col {
    display: flex;
    flex-direction: column;
  }

  .title {
    margin-bottom: 22px;

    font-style: normal;
    font-weight: normal;
    font-size: 17px;
    line-height: 20px;

    color: #000000;
  }

  ol {
    width: 189px;
    padding-left: 0 !important;
  }

  li {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 37px;
    padding: 0 10px;
    margin-top: 0 !important;
    list-style: none;
    transition: transform 0.1s;
    cursor: pointer;
  }

  li:hover {
      transform: scale(1.1);
      transition: transform 0.2s;
  }

  span {
    font-style: normal;
    font-weight: normal;
    font-size: 13px !important;
    line-height: 16px;
  }
`}</style>

<div class="container">
    <div class="col">
        <h1 class="title">Primary Color</h1>
        <ol>
            {Object.entries(colors.primary).map(([key, value]) => (
                <li
                    key={key}
                    style={{ backgroundColor: value, color: key < 500 ? '#000' : '#fff' }}
                    onClick={() => {
                        copyToClipboard(value);
                        alert('복사 되었습니다.');
                    }}
                >
                    <span>{value}</span>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="col">
        <h1 class="title">Success Color - Blue</h1>
        <ol>
            {Object.entries(colors.success).map(([key, value]) => (
                <li
                    key={key}
                    style={{ backgroundColor: value, color: key < 500 ? '#000' : '#fff' }}
                    onClick={() => {
                        copyToClipboard(value);
                        alert('복사 되었습니다.');
                    }}
                >
                    <span>{value}</span>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="col">
        <h1 class="title">Error Color</h1>
        <ol>
            {Object.entries(colors.error).map(([key, value]) => (
                <li
                    key={key}
                    style={{ backgroundColor: value, color: key < 500 ? '#000' : '#fff' }}
                    onClick={() => {
                        copyToClipboard(value);
                        alert('복사 되었습니다.');
                    }}
                >
                    <span>{value}</span>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
    <div class="col">
        <h1 class="title">Black&#38;Grey</h1>
        <ol>
            {Object.entries(colors.grey).map(([key, value]) => (
                <li
                    key={key}
                    style={{ backgroundColor: value, color: key < 500 ? '#000' : '#fff' }}
                    onClick={() => {
                        copyToClipboard(value);
                        alert('복사 되었습니다.');
                    }}
                >
                    <span>{value}</span>
                    <span>{key}</span>
                </li>
            ))}
        </ol>
    </div>
</div>
